{
  "version": 2,
  "builds": [
    {
      "src": "src/**/*.ts",
      "use": "@now/node",
      "config": { "tsConfig": "tsconfig.json" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/app.js"
    }
  ]
}
